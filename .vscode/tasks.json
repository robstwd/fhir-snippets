{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Package VS Code extension with VSCE",
            "type": "shell",
            "command": "vsce package",
            "group": {
                "kind": "build",
                "isDefault": true
            }

        }
    ]
}